# TODO - Dashboard updates

## Step 1: Inspect current implementation
- [x] Read `src/pages/Dashboard.jsx`

## Step 2: Implement requested UI/data mapping changes
- [x] Replace Row 4 to pass only `score` and `job_fit` keys (wrap `result`)
- [x] Replace Row 5 to pass `strengths`, `weaknesses`, `interview_probability` (wrap `result`)

## Step 3: Implement UX improvements
- [x] Add Auto Scroll in `handleAnalyze()` after `setResult(response.data)`
- [x] Add Reset button below Analyze button (resets resumeFile/jdFile/result)

## Step 4: Verify code builds
- [ ] Run lint/build (if configured)


